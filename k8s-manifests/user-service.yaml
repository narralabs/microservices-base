apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service-k8s-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-service-k8s-pod
  template:
    metadata:
      labels:
        app: user-service-k8s-pod
    spec:
      containers:
        - name: user-service
          image: narralabs/user-service
          ports:
            - containerPort: 7000
          env:
            - name: MONGODB_URL
              value: mongodb://mongo-service-k8s-service:27017/microservices_development
          readinessProbe:
            tcpSocket:
              port: 7000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 7000
            initialDelaySeconds: 15
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: user-service-k8s-service
spec:
  selector:
    app: user-service-k8s-pod
  ports:
    - protocol: TCP
      port: 7000
      targetPort: 7000
  type: ClusterIP
